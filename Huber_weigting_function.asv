function [ output_val ] = Huber_weigting_function( x, d)
%UNTITLED5 Summary of this function goes here
%   Detailed explanation goes here

%x is a vector 
[num_rows, num_columns]=size(x); 


if abs(a)>d
    output_val=0.5*a^2; 
else
    output_val=d*(abs(a)-0.5*d); 
end

